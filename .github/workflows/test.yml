# PHP lint, Composer lockfile audit, PHPUnit units (Testing Foundation, ADR 0018).
#
# Comprobar no es desplegar: no FTPS, no Pages, no production environment,
# no secrets. Does not close D12b (security-audit automation). WordPress
# integration remains isolated tools/qa-*.sh until a PHPUnit integration
# suite is added. composer audit --locked covers root Composer deps only.
name: Tests

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  php-lint-and-unit:
    name: PHP lint, Composer audit, and unit (PHP 8.3)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.3"
          extensions: mbstring
          coverage: none
          tools: composer

      - name: Cache Composer packages
        uses: actions/cache@v6
        with:
          path: ~/.composer/cache
          key: composer-${{ runner.os }}-8.3-${{ hashFiles('composer.lock') }}
          restore-keys: |
            composer-${{ runner.os }}-8.3-

      - name: Install development dependencies
        run: composer install --no-interaction --prefer-dist --no-progress

      - name: Validate composer.json and composer.lock
        run: composer validate --no-check-publish

      - name: PHP syntax lint
        run: composer lint:php

      - name: Composer dependency audit
        run: composer audit --locked

      - name: Run unit tests
        run: composer test:unit
